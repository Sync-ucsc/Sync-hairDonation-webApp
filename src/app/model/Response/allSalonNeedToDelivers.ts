export interface AllSalonNeedToDelivers {
  requestId: string,
  status: string,
  address: string,
  createdAt: string,
  salonId: string,
  salonEmail: string,
  salonName: string,
  lat: number,
  lng: number
}
